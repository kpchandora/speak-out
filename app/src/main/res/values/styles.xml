<resources xmlns:tools="http://schemas.android.com/tools">

    <declare-styleable name="Theme">
        <attr name="colorControlLight" format="color" />
    </declare-styleable>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <!--    android:style/Theme.Dialog-->

    <style name="Theme.UserDialog" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">
            @android:drawable/screen_background_light_transparent
        </item>
    </style>

    <style name="Widget.Speak.AppBar" parent="@style/Widget.Design.AppBarLayout">
        <item name="android:background">?attr/toolbarColor</item>
        <item name="android:elevation">8dp</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/dr_bottomsheet_top_rounded_corners</item>
    </style>

    <style name="ProfileBottomSheet" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
        <item name="android:windowSoftInputMode">stateAlwaysHidden</item>
        <item name="android:layout_marginStart">2dp</item>
        <item name="android:layout_marginEnd">2dp</item>
        <item name="android:windowIsFloating">false</item>
    </style>

    <style name="SearchUserStyle" parent="Widget.AppCompat.SearchView">
<!--        <item name="searchIcon">@drawable/ic_search_unselected</item>-->
        <item name="queryBackground">@drawable/input_bg_dr</item>
        <item name="android:background">@drawable/input_bg_dr</item>
        <item name="queryHint">@string/search_here</item>
    </style>

</resources>
